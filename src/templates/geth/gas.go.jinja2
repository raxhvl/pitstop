// {{ pitstop_header }}
// Gas schedule: {{ schedule.fork }}
// {{ schedule.description }}

package vm

// Operations gas costs
const (
{%- for name, cost in schedule.operations.items() %}
	Gas{{ name }} uint64 = {{ cost }}
{%- endfor %}
)

// Storage gas costs
const (
{%- for name, cost in schedule.storage.items() %}
	Gas{{ name }} uint64 = {{ cost }}
{%- endfor %}
)

// Precompile gas costs
const (
{%- for name, cost in schedule.precompiles.items() %}
	Gas{{ name }} uint64 = {{ cost }}
{%- endfor %}
)

// Memory gas costs
const (
{%- for name, cost in schedule.memory.items() %}
	Gas{{ name }} uint64 = {{ cost }}
{%- endfor %}
)
